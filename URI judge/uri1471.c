#include<stdio.h>
int main()
{
    int a,b,c[10000];
    int i;
    while(scanf("%d %d",&a,&b)!=EOF)
    {
        if(a==b) printf("*\n");
        else {
            for(i=1;i<=b;i++)
            {
                if()
            }

        }
    }
}
